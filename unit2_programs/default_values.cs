using System;
class Program{
    static void Main (String[] args){
        int i = default(int);
        char ch = default(char);
        bool is_valid = default(bool);
        float height = default(float);
        Console.WriteLine(i);
        Console.WriteLine(ch);
        Console.WriteLine(is_valid);
        Console.WriteLine(height);
    }
}